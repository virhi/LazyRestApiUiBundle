{% if field.key is defined and field.key == true %}
    <div class="form-group">
        <label class="col-sm-2 control-label" for="{{ field.name }}">{{ field.name }} :</label>
        <div class="col-sm-10">
            <textarea rows="4" cols="50" type="{{ field.type }}" readonly name="{{ field.name }}" class="form-control" id="{{ field.name }}" placeholder="{{ field.value }}" value="{{ field.value }}">{{ field.value }}</textarea>
        </div>
    </div>
{% else %}
    <div class="form-group">
        <label class="col-sm-2 control-label" for="{{ field.name }}">{{ field.name }} :</label>
        <div class="col-sm-10">
            <textarea rows="4" cols="50" type="{{ field.type }}" name="{{ field.name }}" class="form-control" id="{{ field.name }}" placeholder="{{ field.value }}" value="{{ field.value }}">{{ field.value }}</textarea>
        </div>
    </div>
{% endif %}